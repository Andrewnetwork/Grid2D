@tool
extends Node2D

func _ready():
	#var grid = Grid2D.new()
	#var grid_item = GridItem.new(grid)
	#var ni = GridImage.new(grid)
	#grid.grid_overlay = true
	#add_child(grid)
	#ni.make_color_rect(Color(0, 1, 0, 0.5), 50,50)
	#grid_item.add_child(ni)
	#grid.add_child(grid_item)
	pass
